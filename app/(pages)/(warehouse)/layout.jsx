import SideBarSection from "@/components/layout/SideBarSection";

export default function WarehouseLayout({ children }) {
  return <SideBarSection sectionKey="warehouse">{children}</SideBarSection>;
}
